<div class="mlb-icons" style="background-color: {{ section.settings.bg_color }};">
	<div class="container">
		<div class="mlb-icons__wrapper">
			{% for block in section.blocks %}
				<div class="mlb-icons__item" {{ block.shopify_attributes }}>
					{% if block.settings.icon != blank %}
						<img class="mlb-icons__item-img"
						     src="{{ block.settings.icon | img_url: 'master' }}"
						     alt="{{ block.settings.icon.alt }}"
						     loading="lazy">
					{% endif %}
					{% if block.settings.title != blank %}
						<div class="mlb-icons__item-title">{{ block.settings.title }}</div>
					{% endif %}
				</div>
			{% endfor %}
		</div>
	</div>
</div>

{% schema %}
{
  "name": "MLB - Icons",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color"
    }
  ],
  "blocks": [
    {
      "type": "icon",
      "name": "Icon",
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Icon"
        },
        {
          "type": "richtext",
          "id": "title",
          "label": "Title"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "MLB - Icons",
      "blocks": [
        {
          "type": "icon"
        },
        {
          "type": "icon"
        },
        {
          "type": "icon"
        },
        {
          "type": "icon"
        },
        {
          "type": "icon"
        }
      ]
    }
  ]
}
{% endschema %}